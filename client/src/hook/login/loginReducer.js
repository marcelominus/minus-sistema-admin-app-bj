//Importamos los TYPES

//ZONA DE SWITCH
export default (state, action) => {
    switch (action.type) {
        // case value:
        //     return {

        //     }
        default:
            return state;
    }
}